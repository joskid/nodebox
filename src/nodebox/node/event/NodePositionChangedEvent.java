package nodebox.node.event;

import nodebox.node.Node;
import nodebox.node.NodeEvent;

public class NodePositionChangedEvent extends NodeEvent {

    public NodePositionChangedEvent(Node source) {
        super(source);
    }

}
